# Bastion mesh E2E config: bob node (09-04)

[agent]
default_model = "llama3"
daily_budget_usd = 0.50

[session]
db_path = "/data/bastion-b.db"
autocompact_threshold = 0.80
keep_last_n = 50

[logging]
log_path = "/data/bastion-b.log"

[mcp]
tool_call_timeout_secs = 30

[channels.telegram]
enabled = false

[channels.webhook]
enabled = true

[mesh]
sync_interval = 1

[[mesh.peer]]
owner_id = "alice"
peer_url = "http://bastion-a:8080"
age_pubkey = "ALICE_AGE_PUBKEY_PLACEHOLDER"
allowed_tags = ["test"]
